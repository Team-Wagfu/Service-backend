{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "clean __pycache__",
            "type": "shell",
            "command": "find ${workspaceFolder} -iname __pycache__ -exec echo '{}'>>somefile.txt\\;",
            "problemMatcher": []
        }
    ]
}